{
  "name": "vpn-tunnel-enforcer",
  "version": "1.1.8",
  "description": "Force all traffic through VPN Happ — Hard (TUN) or Soft (Autoconfig) mode with IP leak monitoring",
  "main": "./out/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:coverage": "vitest --run --coverage",
    "test:vpn-profiles": "node scripts/test-vpn-profiles.mjs",
    "preview": "electron-vite preview",
    "build:sidecar": "node scripts/build-sidecar.mjs",
    "build:snapshot": "node scripts/generate-v8-snapshot.mjs",
    "dist": "npm run build:sidecar && npm run build:snapshot && npm run build && electron-builder",
    "dist:win": "npm run build:sidecar && npm run build:snapshot && npm run build && electron-builder --win nsis",
    "dist:portable": "npm run build:sidecar && npm run build:snapshot && npm run build && electron-builder --win portable"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "electron-store": "^8.1.0",
    "flag-icons": "^7.5.0",
    "socks": "^2.8.3",
    "sudo-prompt": "^9.2.1",
    "tldts": "^7.4.8"
  },
  "devDependencies": {
    "@dnd-kit/core": "^6.3.1",
    "@dnd-kit/sortable": "^10.0.0",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/node": "^22.0.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "@vitest/coverage-v8": "^4.1.6",
    "autoprefixer": "^10.4.19",
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.1.1",
    "electron": "^42.0.0",
    "electron-builder": "^26.0.0",
    "electron-vite": "^3.0.0",
    "fast-check": "^4.8.0",
    "framer-motion": "^11.1.0",
    "i18next": "^26.2.0",
    "jsdom": "^29.1.1",
    "lucide-react": "^0.378.0",
    "postcss": "^8.4.38",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-i18next": "^17.0.8",
    "tailwind-merge": "^2.3.0",
    "tailwindcss": "^3.4.3",
    "typescript": "^5.4.0",
    "vitest": "^4.1.6",
    "zustand": "^4.5.2"
  },
  "overrides": {
    "picomatch": "^3.0.1"
  }
}
