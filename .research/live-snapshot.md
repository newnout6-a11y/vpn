# Срез deep-research (снят с транскриптов, без повторного прогона)

Снято: 2026-08-29T13:57:14.367Z
Транскриптов: 280 · извлечённых утверждений: 225 · получивших голоса: 75
Подтверждено: 26 · опровергнуто: 46 · **не рассмотрено: 3**

> Не рассмотренные — это НЕ опровергнутые. У них все голосующие агенты не вернули вердикт
> (упали, либо ещё считают). Читать как «неизвестно», а не как «ложно».

## Подтверждено (26)

### Same-document View Transitions (document.startViewTransition) have shipped since Chrome 111, so they are unconditionally available in Electron 42's Chromium — no feature flag or polyfill needed for page-switch and theme-switch transitions in this SPA. Later refinements view-transition-class and :active-view-transition-type() require Chrome/Edge 125+.
**Голоса:** 3-0 · **Источник:** https://developer.chrome.com/docs/web-platform/view-transitions/same-document (primary)
> in Chrome as of Chrome 111.
**Разбор верификатора:** Tried to refute on three axes; all failed.  1) Quote fidelity. The primary source does say it verbatim: "Same-document view transitions are supported in Chrome as of Chrome 111." (https://developer.chrome.com/docs/web-platform/view-transitions/same-document). The abbreviated supporting quote "in Chrome as of Chrome 111" is not a misread or a crop that changes meaning.  2) The version arithmetic, verified independently of the claim. Electron 42 ships Chromium 148: "Electron 42 has been released! It includes upgrades to Chromium 148.0.7778.96, V8 14.8, and Node v24.15.0" (https://www.electronjs.org/ru/blog/electron-42-0), corroborated by https://releases.electronjs.org/release/v42.0.0 and v42.
### During the startViewTransition callback the page is frozen: any await inside it (fetch, font loading, heavy React render) stalls the whole UI, so data must be fetched before starting the transition and any wait must be raced against an aggressive timeout (the docs' example races document.fonts.ready against 100ms). This is the concrete cost/failure mode for using view transitions on route changes in a persistently open desktop app.
**Голоса:** 3-0 · **Источник:** https://developer.chrome.com/docs/web-platform/view-transitions/same-document (primary)
> the page is frozen, so delays here should be kept to a minimum.
**Разбор верификатора:** Claim verified against the cited primary source plus the W3C spec; both corroborate it, and no credible source disputes it.  1) Cited source supports every element of the claim, not just the pull-quote. developer.chrome.com/docs/web-platform/view-transitions/same-document, section "Async DOM updates, and waiting for content": the callback "can return a promise, which allows for async DOM updates, and waiting for important content to be ready"; "The transition won't be started until the promise fulfills"; "the page is frozen, so delays here should be kept to a minimum." It explicitly says "network fetches should be done before calling .startViewTransition(), while the page is still fully inte
### Native acrylic is not uniformly cheap across Windows versions: it needs Windows 10 build >17763, and the library itself warns that performance degrades on builds older than Windows 11 22000 — so 'the OS composites it, not Chromium' does not guarantee the perf win on Windows 10 targets.
**Голоса:** 3-0 · **Источник:** https://github.com/SoFriendly/vibe (primary)
> Acrylic: Windows 10 builds after 17763. The README warns "performance may suffer on builds earlier than Windows 11 22000."
**Разбор верификатора:** QUOTE VERIFIED VERBATIM. Fetched https://github.com/SoFriendly/vibe README: "the Acrylic effect is supported in Windows 10 builds later than build 17763 (though performance may suffer on builds earlier than Windows 11 22000), and the Mica effect is supported in Windows 11 only". Both the build-number gate (17763) and the perf caveat (pre-22000) are present exactly as the claim states, and the claim's conclusion is hedged appropriately ("does not guarantee the perf win"), not overstated into a measured benchmark.  INDEPENDENT CORROBORATION of the underlying mechanism (so the claim does not rest on a single README parenthetical): 1. https://github.com/tauri-apps/window-vibrancy/issues/47 (open
### Layer promotion via will-change is not free — each promoted layer consumes GPU memory — and repaint cost scales with layer area, so the mitigation for paint-heavy effects is to shrink the painted layer rather than to promote more elements. Creating a 3D transform context promotes more elements than expected.
**Голоса:** 3-0 · **Источник:** https://motion.dev/docs/performance (primary)
> Hint at a new layer with `element.style.willChange = "transform"`, but sparingly: "Creating new layers doesn't come for free. Each takes space on the GPU." ... "the time a browser takes to repaint a layer is proportional to its size." ... creating a 3D context "promotes more elements than you might expect."
**Разбор верификатора:** Attempted refutation on all five checklist axes; none succeeded.  1) Quote fidelity — verified by fetching the primary source directly (https://motion.dev/docs/performance). It contains, verbatim: "Creating new layers doesn't come for free. Each takes space on the GPU. So do so sparingly."; "the time a browser takes to repaint a layer is proportional to its size" followed by "you can improve the performance of these animations by making smaller layers"; and on 3D: "3D transforms ride the same compositor path, but establishing a 3D context promotes more elements than you might expect." The claim's only inferential step — "the mitigation is to shrink the painted layer rather than promote more 
### Chrome DevTools' Rendering tab provides a Paint flashing checkbox that visually marks every repaint, making it the direct way to detect continuously repainting elements (e.g. an infinite shimmer keyframe or pulse-ring animation) — if the whole viewport or a supposedly static region flashes green, repaint work is happening that shouldn't be.
**Голоса:** 3-0 · **Источник:** https://developer.chrome.com/docs/devtools/rendering/performance (primary)
> Paint flashing — Reveals repaint activity. When enabled, "Chrome flashes the screen green whenever repainting happens."
**Разбор верификатора:** Attempted refutation on all five checklist axes; the claim survives.  1. Quote support — near-verbatim, not an overreach. I fetched the cited primary source (https://developer.chrome.com/docs/devtools/rendering/performance). It documents enabling the **Paint flashing** checkbox in the Rendering tab, states "With this option switched on, Chrome flashes the screen green whenever repainting happens," and adds the exact interpretive guidance the claim asserts: "If, on another page, you see the whole screen flash green or areas of the screen that you didn't think should be painted, consider investigating further." The claim's second half is essentially a paraphrase of that sentence, so the "if th
### The Frame rendering stats overlay reports four measurable signals at once: a real-time FPS estimate, a frame timeline classifying frames as successfully rendered (blue), partially presented (yellow), or dropped (red), whether GPU rasterization is on or off, and GPU memory used versus maximum in MB — so both dropped-frame counts and GPU-acceleration state can be verified empirically rather than assumed.
**Голоса:** 3-0 · **Источник:** https://developer.chrome.com/docs/devtools/rendering/performance (primary)
> Frame rendering stats — A live overlay pinned to the viewport's top-right corner. It reports four things: a real-time FPS estimate while the page runs; a frame timeline plotting three categories — successfully rendered frames as blue lines, partially presented frames as yellow, and dropped frames as red; GPU raster status ("on or off"); and GPU memory as used-versus-maximum MB.
**Разбор верификатора:** Attempted refutation failed on the core enumeration; it is verbatim-supported by the primary source, retrieved twice independently.  1) Quote-support check — PASSES. I fetched the live doc (https://developer.chrome.com/docs/devtools/rendering/performance) and, separately, the upstream source markdown for the same page (https://raw.githubusercontent.com/GoogleChrome/developer.chrome.com/main/site/en/docs/devtools/rendering/performance/index.md). Both independently return the same four-item list for the Frame rendering stats overlay: (a) "Real time estimate of frames per second as the page runs"; (b) a frame timeline plotting three frame types — successfully rendered = blue lines, "Partially p
### Layer Borders overlays the compositing structure directly on the page (orange/olive layer borders, cyan tiles), with the authoritative color legend in Chromium's debug_colors.cc — this is the check for whether an element was actually promoted to its own compositing layer (relevant to claims that backdrop-filter or will-change promotion reduces cost).
**Голоса:** 3-0 · **Источник:** https://developer.chrome.com/docs/devtools/rendering/performance (primary)
> Layer Borders — Draws a compositing overlay on the page. Borders render in orange and olive; tiles render in cyan. For the full color legend, the docs point at Chromium's `debug_colors.cc` source file
**Разбор верификатора:** Verified against both the cited primary source and the Chromium source it points to.  1) Primary source confirms the quote verbatim. https://developer.chrome.com/docs/devtools/rendering/performance states: "Use Layer Borders to view an overlay of layer borders and tiles on top of the page." Instructions: "Open the Rendering tab and check Layer Borders. Observe layer borders in orange and olive and tiles in cyan. See the comments in debug_colors.cc for an explanation of the color-codings." The doc links cs.chromium.org/chromium/src/cc/debug/debug_colors.cc. Mirror of the same doc (chrome.jscn.org/docs/devtools/rendering/performance/) reproduces identical wording, so this is not a fetch artifa
### content-visibility: hidden preserves cached rendering state for inactive views (unlike display:none, which destroys it); Facebook measured up to 250ms faster navigation when returning to cached SPA views — applicable to Dashboard/Servers/Settings page switching in this Electron SPA.
**Голоса:** 2-1 · **Источник:** https://web.dev/articles/content-visibility (primary)
> up to 250ms improvement in navigation times when going back to previously cached views
**Разбор верификатора:** VERIFIED, with a magnitude caveat.  1) Quote fidelity — exact. Fetched https://web.dev/articles/content-visibility: the page contains verbatim "Facebook engineers observed an up to 250ms improvement in navigation times when going back to previously cached views," in a note inside the `content-visibility: hidden` section. The claim reproduces it faithfully and keeps the "up to" hedge. The same section frames the benefit as retaining "the benefits of cached rendering state" and explicitly names the SPA use case: "Inactive app views can be left in the DOM with content-visibility: hidden applied to prevent their display." So the SPA framing is the article's own, not the claim author's extrapolat
### Measured rendering cost on a chunked-sections demo dropped from 232ms to 30ms (~7x), with a stated general expectation of 50%+ reduction in load-time rendering cost — a cheap win needing only two CSS declarations per item, unlike blur/shimmer effects that added permanent compositing cost.
**Голоса:** 2-1 · **Источник:** https://web.dev/articles/content-visibility (primary)
> a boost from a 232ms rendering time to a 30ms rendering time
**Разбор верификатора:** VERIFIED AGAINST PRIMARY SOURCE. Fetched https://web.dev/articles/content-visibility directly. Both numbers are verbatim and correctly scoped by the claim:  1) "232ms to 30ms" — real, exact. Article: "rendering times going from 232ms to 30ms on initial page load", described as a "7x rendering performance boost on initial load", measured on the article's own travel-blog CodePen demo with `content-visibility: auto; contain-intrinsic-size: 1000px` on chunked `.story` sections. "Rendering time" = style/layout/paint work during load, not total load time. The claim correctly labels this "a chunked-sections demo" rather than generalizing it.  2) "50%+" — real, and the claim's hedge is accurate. Art
### As of this Chrome team post, the CSS properties hardware-accelerated (compositor-driven) by default were only opacity, filter, and transform — meaning any "living" indicator animated via other properties (box-shadow, width, background-position shimmer) runs on the main thread and repaints every frame. Dated 2021, so this is a floor, not the current Chromium set.
**Голоса:** 3-0 · **Источник:** https://developer.chrome.com/blog/hardware-accelerated-animations (primary)
> the current CSS properties that are hardware-accelerated by default only include `opacity`, `filter`, and `transform`
**Разбор верификатора:** VERIFIED against the primary source. I fetched https://developer.chrome.com/blog/hardware-accelerated-animations directly. The sentence exists verbatim: "the current CSS properties that are hardware-accelerated by default only include `opacity`, `filter`, and `transform`". Page carries a last-updated stamp of 2021-02-22 UTC, exactly the date the claim asserts. Source is a Chrome team (developer.chrome.com) post — primary, first-party, appropriate strength for a statement about Chromium internals.  Attack 1 — truncated quote / overreach? The full sentence continues "...for now, background-color and clip-path will soon join the list." The claim omits that continuation, which is the only real w
### Frameless Windows mica/acrylic (`backgroundMaterial`) only works because Electron carries a downstream Chromium patch forcing frameless translucent windows to become "activated" — the state the DWM effects require. It landed in Electron 27 (merged Sep 11 2023, backported via #39802), so it is available in Electron 42, but it is not upstream Chromium behavior.
**Голоса:** 2-1 · **Источник:** https://github.com/electron/electron/pull/39708 (primary)
> Adds a Chromium patch allowing frameless, translucent windows — mica and acrylic — to become "activated", which the effects require. ... The patch file is `patches/chromium/fix_activate_background_material_on_windows.patch`. Its own header notes it "likely can't be upstreamed as-is", because Chromium doesn't currently consider this use case.
**Разбор верификатора:** Attempted refutation on four fronts; the claim survives all of them, though one detail (the patch *filename*) is stale.  1. Quote/source fidelity — CONFIRMED against the primary source. WebFetch of https://github.com/electron/electron/pull/39708 reproduces: adds a Chromium patch letting frameless translucent windows (mica/acrylic) become "activated", which the effects require; patch file `patches/chromium/fix_activate_background_material_on_windows.patch`; header states "This patch likely can't be upstreamed as-is, as Chromium doesn't have this use case in mind currently."; merged by codebytere as d182794 into main on Sep 11 2023; trop auto-backported to 27-x-y as PR #39802; release note "Fi
### The Chromium feature author explicitly characterizes backdrop-filter as potentially very performance-demanding, and the primary design goal was merely to not excessively slow down sites — not to be cheap. Treating it as a free visual effect is unsupported by the feature's own explainer.
**Голоса:** 2-1 · **Источник:** https://github.com/mfreed7/backdrop-filter-feature/blob/master/README.md (primary)
> can be very performance-demanding
**Разбор верификатора:** Verified both load-bearing statements verbatim against the primary source (https://github.com/mfreed7/backdrop-filter-feature/blob/master/README.md), fetched directly:  1. "Note that this feature can be very performance-demanding." — exact match to the supporting quote. Context in the explainer reinforces rather than softens it: the sentence is used to explain why "there are no effective polyfills - none can be built that are fast enough" and that alternatives "are too slow for production use."  2. The design-goal framing is also literal. The explainer's stated goal is "A performant implementation that does not excessively slow down sites that use it." That is a deliberately modest bar — "do
### Mica requires Windows 11 and silently falls back to a solid theme color on Windows 10, whereas Desktop Acrylic is supported from Windows 10 build 17763 onward — meaning a Mica-only design has no translucency at all on Win10 machines.
**Голоса:** 3-0 · **Источник:** https://learn.microsoft.com/en-us/windows/apps/develop/ui/materials (primary)
> **Windows version**: Windows 11 only. Falls back to a solid theme color on Windows 10.
**Разбор верификатора:** Claim is directly and verbatim supported by the cited primary source, and independently cross-confirmed.  1) Primary source verified by fetch: https://learn.microsoft.com/en-us/windows/apps/develop/ui/materials (page metadata shows ms.date / updated_at 2026-08-19, i.e. current, not stale). Mica section: "Windows version: Windows 11 only. Falls back to a solid theme color on Windows 10." Desktop Acrylic section: "Windows version: Windows 10 (build 17763) and later." Both halves of the claim are quoted text, not inference.  2) Independent corroboration: MS UWP docs (https://docs.microsoft.com/en-us/windows/uwp/ui-input/mica-uwp) — "Mica is available for UWP apps that use WinUI 2 while running 
### Layer promotion (via will-change: transform) does isolate a frequently repainted element from the rest of the page, but each promoted layer consumes memory and compositor management overhead, and the Chrome team's explicit guidance is not to promote without first profiling — i.e. promoting a glass panel is a measurable tradeoff, not a free fix.
**Голоса:** 3-0 · **Источник:** https://web.dev/articles/simplify-paint-complexity-and-reduce-paint-areas (primary)
> each layer requires both memory and management … Don't promote elements without profiling.
**Разбор верификатора:** Verified the primary source directly (WebFetch of https://web.dev/articles/simplify-paint-complexity-and-reduce-paint-areas, author Paul Lewis, Google Chrome DevRel, page still live on web.dev). All three factual components appear verbatim or near-verbatim:  1. Isolation: the article states the browser renders into several compositor layers so that "elements that are regularly repainted, or are moving on screen with transforms" stay isolated from the rest, and that "The best way to create a new layer is to use the will-change CSS property" with "a value of transform". Claim's first clause is accurate, not an overreach.  2. Cost: "Care must be taken not to create too many layers, however, as 
### Whether an animation is actually compositor-driven can be verified empirically via the DevTools Rendering panel's Paint flashing option (and the Performance panel timeline) — a concrete measurement procedure rather than guesswork.
**Голоса:** 3-0 · **Источник:** https://developer.chrome.com/blog/hardware-accelerated-animations (primary)
> We can test this in the DevTools Rendering panel, when turning on Paint flashing.
**Разбор верификатора:** Verified the primary source directly. https://developer.chrome.com/blog/hardware-accelerated-animations does contain the quoted sentence and uses the procedure exactly as the claim describes: "We can test this in the DevTools Rendering panel, when turning on Paint flashing," with the discriminating observation being "You can see that on the left (Chromium 87), repaint occurs each time the spinner animates (which is continuously)" versus "there is no repainting (Chromium 89 and Firefox)." It also names the second half of the claim's procedure: "Taking a closer look at the Performance panel, you can again see this effect," and "You avoid rendering and painting time completely for the animation
### @starting-style and transition-behavior: allow-discrete both shipped in Chromium 117 (Chrome/Edge 117) and reached Baseline Newly available on 2024-08-06, so they are safely usable in any Electron built on Chromium 117+ (Electron 42's Chromium is far newer) without polyfills.
**Голоса:** 3-0 · **Источник:** https://web.dev/blog/baseline-entry-animations (primary)
> Two of the four entry/exit features covered in Chrome's earlier post reached Baseline Newly available on August 6, 2024, triggered by the Firefox 129 release: `@starting-style` and `transition-behavior: allow-discrete`.
**Разбор верификатора:** Tried to refute on all five checklist axes; every one held.  1) Quote support. The supporting quote covers the Baseline half exactly ("reached Baseline Newly available on August 6, 2024, triggered by the Firefox 129 release"). Fetching the primary source in full also confirms the Chromium half, which the quote alone did not: the article's own support tables list `@starting-style` — Chrome 117, Edge 117, Firefox 129, Safari 17.5; `transition-behavior` — Chrome 117, Edge 117, Firefox 129, Safari 17.4. So "both shipped in Chromium 117" is stated by the source, not inferred. No overreach found.  2) Independent corroboration (not the same source re-quoted). https://caniuse.com/mdn-css_at-rules_st
### CSS anchor positioning shipped in Chromium 125 (Chrome and Edge 125), which means it is available unflagged in the Chromium bundled with Electron 42 — usable for MacSelect/tooltip/popover placement without a JS positioning library.
**Голоса:** 3-0 · **Источник:** https://developer.chrome.com/docs/css-ui/anchor-positioning-api (primary)
> Chrome 125 and Edge 125 ... Because browser support is limited at this time, you likely want to use this API with some precautions.
**Разбор верификатора:** Tried to refute on four fronts; all failed. (1) Ship version verified at the primary source: https://developer.chrome.com/release-notes/125 lists "CSS anchor positioning" under CSS for Chrome 125 stable (2024-05-14) with no flag, no origin trial, no staged rollout noted (contrast: the mousemove entry on the same page explicitly flags a gradual rollout). Properties named: anchor-name, position-anchor, anchor(), inset-area. (2) The Electron 42 inference is not hand-waved — I verified it in this repo, not from memory. C:\Users\Redmi\CascadeProjects\vpn\vpn-tunnel-enforcer\package.json pins "electron": "^42.0.0"; C:\Users\Redmi\CascadeProjects\vpn\vpn-tunnel-enforcer\node_modules\electron\packag
### Mica is explicitly engineered for low cost: it samples the desktop wallpaper only once to build its visualization, rather than continuously re-sampling what is behind the window. Microsoft therefore recommends it for the persistently visible app foundation layer — the opposite cost profile from a permanently-live CSS backdrop-filter, which re-blurs its backdrop whenever the layer is dirtied.
**Голоса:** 3-0 · **Источник:** https://learn.microsoft.com/en-us/windows/apps/develop/ui/system-backdrops (primary)
> Mica is an opaque material that incorporates the user's theme and desktop wallpaper to create a highly personalized appearance. Mica is designed for performance as it only captures the background wallpaper once to create its visualization, so we recommend it for the foundation layer of your app, especially in the title bar area.
**Разбор верификатора:** VERIFIED, with two qualifications the synthesizer must carry forward.  1) Core assertion is verbatim in a current first-party doc. Fetched https://learn.microsoft.com/en-us/windows/apps/develop/ui/system-backdrops (page metadata: ms.date / updated_at 2026-08-19, i.e. ten days old): "Mica is an opaque material that incorporates the user's theme and desktop wallpaper... Mica is designed for performance as it only captures the background wallpaper once to create its visualization, so we recommend it for the foundation layer of your app, especially in the title bar area." Both halves of the claim's Microsoft-sourced content (once-only sampling; recommended for the foundation layer) are exact, no
### The Chromium feature author explicitly concedes backdrop-filter is inherently performance-demanding, even while arguing the shipped design avoids a significant penalty — so 'it shipped in Chromium' is not evidence it is cheap.
**Голоса:** 3-0 · **Источник:** https://github.com/mfreed7/backdrop-filter-feature/blob/master/README.md (primary)
> Note that this feature can be very performance-demanding
**Разбор верификатора:** Attempted refutation on all five checklist axes; the claim survives all of them.  1) QUOTE SUPPORT — verified in the primary source, both halves present. Two independent fetches of https://github.com/mfreed7/backdrop-filter-feature/blob/master/README.md (HTML) and the raw file confirm the same sentences. The concession appears immediately after the Goals list: "Note that this feature can be very performance-demanding", elaborated as "This is part of the reason why there are no effective polyfills - none can be built that are fast enough" and, of existing workarounds, "all are too slow for production use." The counter-argument the claim describes is also literally there — the Solution section
### The frameless-mica/acrylic path has a multi-year tail of regressions after this PR — titlebar incorrectly shown on frameless windows (#40395, #40749), maximizing broken with Mica (#45456, Feb 2025), `backgroundMaterial` failing on initial activate (#46657, Apr 2025) — so native Windows materials are not a risk-free drop-in for CSS backdrop-filter.
**Голоса:** 3-0 · **Источник:** https://github.com/electron/electron/pull/39708 (primary)
> #40395 (closed) and #40749 (merged): titlebar incorrectly shown on frameless windows. #45456 (Feb 2025): window maximizing with Mica. #46657 (Apr 2025): `backgroundMaterial` on initial activate.
**Разбор верификатора:** All four cited items verified directly on github.com; each is real, correctly dated, and correctly characterized as part of a regression tail rooted in electron/electron#39708 (clavin, "frameless mica/acrylic windows", Aug 2023).  1) #40395 — "fix: titlebar incorrectly displayed on frameless windows", opened Oct 31 2023, closed by the author ("closed this by accident, following up on #40749"). It targets bug #39959: "deactivating a transparent frameless window will show a title bar unexpectedly." Root cause named in-thread: the #39708 patch (`fix_activate_background_material_on_windows.patch`) routed WM_NCACTIVATE to DefWindowProc, which also drew a titlebar. Reviewer clavin blocked the naiv
### Chrome writes the specific failure reasons for non-composited animations into the DevTools performance trace, and Lighthouse reads that trace to report both the offending DOM nodes and the per-animation failure reason — giving a concrete, repeatable verification method for whether an animation is actually compositor-driven (rather than eyeballing it).
**Голоса:** 3-0 · **Источник:** https://developer.chrome.com/docs/lighthouse/performance/non-composited-animations (primary)
> When Chrome can't composite an animation, it writes the failure reasons into the DevTools trace. Lighthouse consumes that trace and reports the offending DOM nodes together with the reason each animation failed.
**Разбор верификатора:** Attempted refutation on five fronts; the core mechanism survived all of them.  1) Quote fidelity — supported, near-verbatim. I fetched the cited primary page (https://developer.chrome.com/docs/lighthouse/performance/non-composited-animations). It states: "When an animation can't be composited, Chrome reports the failure reasons to the DevTools trace, which is what Lighthouse reads" and "Lighthouse lists DOM nodes which have animations that were not composited along with the failure reason(s)." Both halves of the claim (trace-written reasons; Lighthouse reporting nodes + per-animation reasons) are literally what the source says, not an overreach. Mirrored at https://web.dev/non-composited-ani
### Electron 42 bundles Chromium 148.0.7778.96, V8 14.8 and Node 24.15.0 — which pins the exact web-platform baseline available to the renderer. Chromium 148 is far past the ship points for same-document View Transitions (111), @starting-style + transition-behavior: allow-discrete (117), scroll-driven animations (115), individual transform properties (104), the popover attribute (114) and CSS anchor positioning (125), so all of those can be used without a fallback path.
**Голоса:** 3-0 · **Источник:** https://electronjs.org/blog/electron-42-0 (primary)
> includes upgrades to Chromium 148.0.7778.96, V8 14.8, and Node v24.15.0
**Разбор верификатора:** Every factual component checks out against primary sources, and I verified each ship point independently rather than trusting the quote alone.  Runtime versions: the primary source (https://electronjs.org/blog/electron-42-0, published May 7, 2026) states Electron 42 "includes upgrades to Chromium 148.0.7778.96, V8 14.8, and Node v24.15.0" (prior line was Chromium 146.0.7680.65, Node v24.14.0, V8 14.6). Mirrored by releases.electronjs.org (v42.0.0-beta.8 lists Chromium 148.0.7778.56 / Node 24.15.0 / V8 14.8.178.6). Project match confirmed locally: C:\Users\Redmi\CascadeProjects\vpn\vpn-tunnel-enforcer\package.json line 42 has "electron": "^42.0.0" (also electron-vite ^3.0.0, framer-motion ^11
### Animating CSS custom properties (including @property-typed ones used for per-axis transforms) always triggers paint, making it slower than having a JS library rebuild a transform string once per frame — so the 'use CSS variables for animation' pattern is a repaint trap, not an optimization.
**Голоса:** 1-1 (воздержалось 1) · **Источник:** https://motion.dev/magazine/do-you-still-need-framer-motion (blog)
> they always trigger paint … way slower
**Разбор верификатора:** QUOTE FIDELITY — not a misread. The motion.dev article states both halves verbatim: "the big caveat with animating CSS variables is that they're slow, because they always trigger paint", and, specifically for the case where the variable only feeds a transform, "it's still way slower to animate with CSS variables than building `transform` strings once a frame via a JS library." It repeats the argument for scroll effects: because "they rely on CSS variables they run on the main thread and trigger paint". So the claim is a faithful restatement, not an overreach on the source.  INDEPENDENT CORROBORATION (this is why I do not refute). Bram Van Damme, unaffiliated with Motion, https://www.bram.us/
### Frameless translucent windows only render the mica/acrylic effect if the window can become 'activated', which required an Electron-maintained Chromium patch (fix_activate_background_material_on_windows.patch) — i.e. the capability rests on a downstream fork patch, not stock Chromium behavior.
**Голоса:** 2-0 (воздержалось 1) · **Источник:** https://github.com/electron/electron/pull/39708 (primary)
> A patch allowing frameless, translucent (mica/acrylic) windows to become "activated", described as "necessary for their effects"
**Разбор верификатора:** Substance verified against primary sources; one factual staleness that does not overturn the claim.  1) Quote fidelity / mechanism. The patch's own header text (fetched raw from https://raw.githubusercontent.com/electron/electron/v36.0.0/patches/chromium/fix_activate_background_material_on_windows.patch) reads verbatim: "This patch adds a condition to the HWND message handler to allow windows with translucent background materials to become activated. It also ensures the lParam of WM_NCACTIVATE is set to -1 so as to not repaint the client area, which can lead to a title bar incorrectly being displayed in frameless windows. This patch likely can't be upstreamed as-is, as Chromium doesn't have 
### Enabling vibrancy/translucency effects makes Electron automatically force the window's background color to transparent, so an opaque CSS background on html/body will occlude the native Mica/Acrylic effect — surfaces must stay transparent for the OS compositor to show through.
**Голоса:** 3-0 · **Источник:** https://github.com/electron/electron/pull/39708 (primary)
> Windows created with vibrancy or translucency effects automatically get transparent background colors.
**Разбор верификатора:** VERDICT: core claim survives, but with a real overreach in the second half's sourcing and a loose mechanism description. Not enough to kill it.  1) First clause is directly supported by the primary source. Fetched electron/electron PR #39708 (merged 2023-09-11 by codebytere, backported to 27-x-y, closes #38532 "Support full frame transparency with Mica background material"). Author's own change list includes: "Automatically makes windows created with vibrancy/translucency effects have transparent background colors" and "Removes some caption styling on translucent window effects on Windows when setting background material". Release note: "Fixed Windows Mica/Acrylic background material effects
### Animations that can be handled entirely in the compositor stage do not require the main thread per frame; the ones that cannot are what the Lighthouse audit flags, and they 'require more work and can appear janky' specifically when the main thread is occupied by heavy tasks — the exact failure mode of a long-lived Electron app whose renderer main thread is also doing React work.
**Голоса:** 3-0 · **Источник:** https://developer.chrome.com/docs/lighthouse/performance/non-composited-animations (primary)
> require more work and can appear janky (not smooth) on low-end phones
**Разбор верификатора:** VERIFIED AGAINST PRIMARY SOURCE. I fetched https://developer.chrome.com/docs/lighthouse/performance/non-composited-animations directly. The page states animations can be "handled completely in the compositor stage of the rendering pipeline"; defines the audit's target as "A non-composited animation is any animation that triggers one of the earlier steps in the rendering pipeline (Style, Layout, or Paint)"; and the full sentence — not the truncated one supplied with the claim — reads "Non-composited animations require more work and can appear janky (not smooth) on low-end phones **or when performance-heavy tasks are running on the main thread**." So all three factual limbs (compositor stage s
## Опровергнуто (46)

### Blur-based effects (shadows, and by extension blur filters) paint substantially slower than a plain filled box, even though the CSS declarations look equally cheap — a measurable cost argument for why the removed shimmer/glass effects were expensive.
**Голоса:** 0-3 · **Источник:** https://web.dev/articles/simplify-paint-complexity-and-reduce-paint-areas (primary)
> CSS syntax hides this: `background: red;` and a `box-shadow` with an rgba blur look comparable but differ substantially in cost.
**Разбор верификатора:** Four independent grounds for refutation.  1. The word "measurable" is unsupported by the source. The cited web.dev article contains ZERO measurements. Its actual text is purely qualitative: "anything that involves a blur (like a shadow, for example) is going to take longer to paint than, say, drawing a red box," and the two declarations "don't necessarily look like they have vastly different performance characteristics, but they do." The only number in the article is a ~10ms per-frame budget target, not a measured result. It tells readers to gather their own timings with the DevTools paint profiler. So the source supports "blur costs more than a fill" (directional) but not "substantially" (m
### @starting-style supplies the pre-render starting state that is otherwise missing for elements entering from display: none, which is the specific gap that forces JS-driven mount/unmount wrappers like framer-motion's AnimatePresence.
**Голоса:** 0-3 · **Источник:** https://web.dev/blog/baseline-entry-animations (primary)
> Supplies the pre-render state an element animates out of — mandatory for elements entering from `display: none`, since there is otherwise no starting point.
**Разбор верификатора:** The claim is two clauses joined by a causal assertion. Clause 1 is fine; clause 2 is an overreach the source does not support and that framer-motion's own documentation contradicts.  WHAT THE SOURCE ACTUALLY SAYS (fetched https://web.dev/blog/baseline-entry-animations, published 2024-08-08): "The `@starting-style` rule defines the initial styles on an element before it's rendered on the page." and "This is required for elements that animate in from `display: none`, as they need a state from which to animate in." The page is entirely CSS-scoped and does NOT mention JavaScript animation libraries, React, mount/unmount wrappers, or AnimatePresence anywhere. It also explicitly limits itself to E
### Electron 42 adds a native background-blur API on the view layer, view.setBackgroundBlur(), alongside animatable view.setBounds() — i.e. a compositor/native path to frosted-glass and view transitions that does not require a permanently active CSS backdrop-filter in the renderer.
**Голоса:** 0-3 · **Источник:** https://electronjs.org/blog/electron-42-0 (primary)
> Added animation functionality to `view.setBounds()` and added `view.setBackgroundBlur()`
**Разбор верификатора:** The API's existence checks out; the load-bearing inference does not.  VERIFIED (factual half): https://electronjs.org/blog/electron-42-0 has a "View animations and background blur" highlight and a New Features bullet: "Added animation functionality to `view.setBounds()` and added `view.setBackgroundBlur()`. [#48812]" (Chromium 148.0.7778.96, Node 24.15.0, V8 14.8). Docs (https://www.electronjs.org/docs/latest/api/view) document `view.setBackgroundBlur(blurRadius)` with the note "You must set a background color with an alpha channel (e.g. `#80ffffff`) in order for the blur effect to be visible", and `view.setBounds(bounds[, options])` with `animate` (duration default 250ms, easing linear|ease
### Motion's per-axis/independent transform syntax (e.g. animate(el, { x: 100, scale: 2 }) — the x/y/scale/rotate props used throughout framer-motion codebases) is implemented via CSS variables and is therefore NOT hardware accelerated; to get compositor acceleration you must animate the full transform string (transform: "translateX(100px) scale(2)"). This is directly falsifiable and directly applicable to the project's 17 framer-motion files.
**Голоса:** 0-3 · **Источник:** https://motion.dev/docs/performance (primary)
> Motion's per-axis syntax (`animate(".box", { x: 100, scale: 2 })`) is implemented with CSS variables, and those "are not accelerated, even though they're being applied to `transform`." When acceleration matters, write the full string instead: `transform: "translateX(100px) scale(2)"`.
**Разбор верификатора:** REFUTED on mechanism and on applicability to this project. The quote is real, but the claim overreaches from Motion's imperative `animate()` engine to "the x/y/scale/rotate props used throughout framer-motion codebases," and that extrapolation is false for the version this project actually ships.  1) Mechanism is wrong for framer-motion React. Installed version is framer-motion 11.18.2 (C:\Users\Redmi\CascadeProjects\vpn\vpn-tunnel-enforcer\node_modules\framer-motion\package.json; package.json declares "^11.1.0", not "motion" v12). In C:\Users\Redmi\CascadeProjects\vpn\vpn-tunnel-enforcer\node_modules\framer-motion\dist\es\render\html\utils\build-transform.mjs, per-axis values are compiled i
### Microsoft restricts true frosted-glass Acrylic to transient, light-dismiss surfaces (flyouts, context menus) and does not recommend it as an always-on base layer — a vendor-level design rule matching this project's own removal of permanent backdrop-filter: blur(20px) from .glass. The supported pattern is Mica/opaque for the persistent window body, Acrylic only on short-lived modals and popovers.
**Голоса:** 1-2 · **Источник:** https://learn.microsoft.com/en-us/windows/apps/develop/ui/system-backdrops (primary)
> Acrylic is a semi-transparent material that replicates the effect of frosted glass. It's used only for transient, light-dismiss surfaces such as flyouts and context menus.
**Разбор верификатора:** CORE CLAIM VERIFIED against primary sources, current as of this month.  1) Quote is verbatim and load-bearing. https://learn.microsoft.com/en-us/windows/apps/develop/ui/system-backdrops (ms.date 2026-08-19, updated_at 2026-08-19 — 10 days old, not stale) says exactly: "Acrylic is a semi-transparent material that replicates the effect of frosted glass. It's used only for transient, light-dismiss surfaces such as flyouts and context menus." The word "only" is Microsoft's own, so the claim is not overreaching its quote. Same page, same paragraph: "Mica is designed for performance as it only captures the background wallpaper once to create its visualization, so we recommend it for the foundation
### Frameless windows with Windows Mica/Acrylic backgroundMaterial were broken until this PR, which shipped in Electron 27 (backported to 27-x-y via #39802) — so Electron 42 does support frameless + backgroundMaterial, and the project's frameless -webkit-app-region:drag window is not a blocker for native acrylic/mica.
**Голоса:** 0-3 · **Источник:** https://github.com/electron/electron/pull/39708 (primary)
> Labeled `semver/patch` and `target/27-x-y`; the trop bot backported it automatically to the 27-x-y branch via #39802, so it landed in Electron 27 (and on `main` for 28). ... Author's note: "Fixed Windows Mica/Acrylic background material effects on frameless windows."
**Разбор верификатора:** The claim's causal/chronological core ("broken until this PR, which shipped in Electron 27") is refuted by Electron's own issue tracker, and the project-specific conclusion rests on a false premise about this codebase.  1) PR #39708 did NOT close the frameless-backgroundMaterial problem. It closed #38532 ("Support full frame transparency with Mica background material"), merged Sep 11 2023 (commit d182794), backported to 27-x-y via #39802. But the issue literally titled "'backgroundMaterial' is not work in frameless window" — https://github.com/electron/electron/issues/38466 (filed May 26 2023, reported on 24.4.0) — remained open for ~19 months AFTER Electron 27 and was closed by a different 
### Electron 42 adds a native, main-process blur API at the View level -- view.setBackgroundBlur() -- plus animatable view.setBounds(), offering a system-composited alternative to CSS backdrop-filter for frosted-glass surfaces (relevant precisely because the project previously reverted backdrop-filter: blur(20px) for performance).
**Голоса:** 0-3 · **Источник:** https://www.electronjs.org/blog/electron-42-0 (primary)
> Added animation functionality to `view.setBounds()` and added `view.setBackgroundBlur()`.
**Разбор верификатора:** The API exists, but the claim's load-bearing characterization — "system-composited alternative to CSS backdrop-filter" — is contradicted by the primary source and is an overreach beyond the quote.  1) CONTRADICTED ON MECHANISM (decisive). The author of the implementing PR (electron/electron#51076, "feat: Add view.setBackgroundBlur", merged a1d6918 on 2026-04-30) describes it as using "chromium's layer compositor to blur contents behind a view", built on "View::GetLayer from #48812". That is Chromium's own cc/viz compositor and Skia — the exact same pipeline that makes CSS backdrop-filter expensive — not the OS/DWM compositor. So it is NOT "system-composited". Compare the OS-composited option
### On Windows 11 24H2 with Electron 35.2.1, a frameless BrowserWindow created with backgroundMaterial: "acrylic" loses the native Windows material and falls back to an opaque fill when the user maximizes/restores via non-programmatic paths (drag-to-top, title-bar double-click), while title-bar button maximize/restore preserves it — i.e. native Windows backdrop material is not reliably persistent in frameless windows.
**Голоса:** 0-2 (воздержалось 1) · **Источник:** https://github.com/electron/electron/issues/46753 (primary)
> Windows 11: Material (Acrylic/Mica) and Rounded Corners Inconsistencies in Frameless BrowserWindow Maximize/Restore Actions ... the issue has been partially fixed for cases involving `win.maximize()` and `win.restore()` ... [where material is lost] incorrect background color
**Разбор верификатора:** REFUTED as outdated and as an overreach. Three independent problems:  1) The cited issue is CLOSED and FIXED, and the fix predates the project's Electron version by 6 majors. electron/electron#46753 (opened 24 Apr 2025 by aleversn, Electron 35.2.1, Win11 24H2, labels bug/platform-windows/status:confirmed) was closed by PR electron/electron#47386 "fix: abnormal behavior of windows background material" (codebytere, opened 5 Jun 2025, merged 17 Jul 2025, squash ec828b0, branch background-material-rework). That PR explicitly lists #46753 among the issues it closes (together with #47366, #46882, #43345, #43196) and was backported to 36-x-y, 37-x-y and 38-x-y (manual backports #47956/#47957 on 4 A
### Anchor positioning replaces third-party JS positioning libraries (Popper/floating-ui-style) for layered UI, removing per-frame JS measurement work from popover/tooltip placement.
**Голоса:** 0-3 · **Источник:** https://developer.chrome.com/docs/css-ui/anchor-positioning-api (primary)
> build layered user interfaces without relying on third-party libraries
**Разбор верификатора:** The claim has two halves. Half one ("anchor positioning can replace third-party JS positioning for layered UI") is supported by the quote. Half two ("removing per-frame JS measurement work from popover/tooltip placement") is an overreach the source never makes, and it misdescribes how the libraries it names actually work.  1) Source does not support the perf half. Fetching https://developer.chrome.com/docs/css-ui/anchor-positioning-api : the page's only claims are that you "can build layered user interfaces without relying on third-party libraries" and that it "simplifies complex layout requirements" for menus, tooltips, selects, dialogs. Nothing on the page mentions per-frame measurement, r
### Only transform and opacity are guaranteed to run entirely on the compositor layer in every modern browser; filter and SVG are fully compositor-processed in Chrome and Firefox, while background-color and clip-path acceleration in Chrome is described as still forthcoming. So a 'safe' animation whitelist for an always-open Electron app is transform/opacity (+filter on Chromium).
**Голоса:** 0-3 · **Источник:** https://motion.dev/docs/performance (primary)
> "In every modern browser, `transform` and `opacity` will operate directly on a layer," making them the safest cross-device choices. Chrome and Firefox process `filter` and SVGs entirely on the compositor; Chrome support for `background-color` and `clip-path` is described as coming "soon."
**Разбор верификатора:** Parts 1–2 of the claim are faithful to the source; the load-bearing third part ("so a safe whitelist for an always-open Electron app is transform/opacity + filter on Chromium") is an overreach the source does not support and that Chrome's own docs contradict.  1) Quote fidelity — partially OK. Fetching https://motion.dev/docs/performance confirms the wording: "In every modern browser, `transform` and `opacity` will operate directly on a layer"; "Chrome and Firefox handle `filter` and SVGs entirely on the compositor"; "Chrome is adding support for `background-color` and `clip-path` soon." So the first two sentences of the claim are quoted accurately, and the "still forthcoming" part is (surpr
### The bug was closed as a duplicate of crbug.com/40916020 ('Rely on Skia's built-in backdrop filtering in software_renderer.cc'), i.e. the fix is an unlanded refactor of Chromium's software renderer that is still tracked as ongoing work. There is no engine-side fix to wait for in an Electron 42-era Chromium; the only available mitigation is to avoid or bound the effect in application CSS.
**Голоса:** 0-3 · **Источник:** https://issues.chromium.org/issues/473056837 (primary)
> I'm marking this issue as a duplicate. Retrofitting software_renderer is one of my ongoing responsibilities. Please track that issue ( crbug.com/40916020) for updates.
**Разбор верификатора:** Two of the claim's three parts check out; the third — the load-bearing one — is an overreach contradicted by the same primary source.  SUPPORTED PARTS: 1. The duplicate relationship is real and verifiable from two directions. Search-index text of https://issues.chromium.org/issues/473056837/resources reproduces the quote verbatim ("I'm marking this issue as a duplicate. Retrofitting software_renderer is one of my ongoing responsibilities. Please track that issue ( crbug.com/40916020) for updates."), and the reciprocal page https://issues.chromium.org/issues/40916020/dupes lists "Severe performance drop when using backdrop-filter: blur(...)" among its duplicates. Title of 40916020 is confirme
### Only transform and opacity changes avoid the paint step; every other animated CSS property (including background, color, shadows, and filters) forces repaint, and any geometry change forces both layout and paint. This is the technical basis for restricting the VPN app's animations to transform/opacity if repaint cost was the reason the previous redesign was reverted.
**Голоса:** 0-3 · **Источник:** https://web.dev/articles/simplify-paint-complexity-and-reduce-paint-areas (primary)
> Anything other than transforms or opacity will always cause paint. … changing the geometry of any element means its pixels need fixing!
**Разбор верификатора:** REFUTED on three independent grounds: (a) the source is 11 years stale, (b) primary Chromium documentation directly contradicts the property list, (c) the causal conclusion about the VPN redesign revert is a non-sequitur.  1. SOURCE IS OUTDATED — the decisive defect. I fetched https://web.dev/articles/simplify-paint-complexity-and-reduce-paint-areas. Its only date metadata is "Last updated 2015-03-20 UTC" (author Paul Lewis). The quoted line does exist verbatim in spirit ("Changing any property apart from transforms or opacity always triggers paint"), so the claim is not a misread of the text — but the text predates the entire RenderingNG compositing rework. The companion web.dev piece https
### A continuously running non-composited animation repaints on every frame, while the composited equivalent eliminates render and paint work entirely — the mechanism behind cutting infinite keyframes shimmer/pulse-ring, and the criterion for reintroducing motion safely.
**Голоса:** 0-3 · **Источник:** https://developer.chrome.com/blog/hardware-accelerated-animations (primary)
> repaint occurs each time the spinner animates (which is continuously)" ... "there is no repainting (Chromium 89 and Firefox)" ... "You avoid rendering and painting time completely for the animation
**Разбор верификатора:** The claim has a true half and a load-bearing false half. True half: the Chrome blog does say, of a non-accelerated spinner, "repaint occurs each time the spinner animates (which is continuously)" vs. "On the right there is no repainting (Chromium 89 and Firefox)" / "You avoid rendering and painting time completely for the animation". That supports the generic composited-vs-non-composited distinction, and the source is primary and not outdated for Electron 42's Chromium.  Refutation 1 — the attribution to this project is factually wrong. I read the actual commit (C:\Users\Redmi\CascadeProjects\vpn, `git show cfa47b5` touching C:\Users\Redmi\CascadeProjects\vpn\vpn-tunnel-enforcer\src\renderer
### View transition animations are not documented as running off the main thread; the default group animation interpolates width and height, which the Chrome team itself flags as running layout every frame, with the planned optimization still unimplemented as of the last update (Sep 2024). Therefore a morphing/size-changing view transition is NOT a free compositor-only animation and should not be assumed cheaper than a transform/opacity animation.
**Голоса:** 0-3 · **Источник:** https://developer.chrome.com/docs/web-platform/view-transitions/same-document (primary)
> is generally frowned upon in web performance circles as it runs layout per frame
**Разбор верификатора:** The quote is real and correctly transcribed, but a load-bearing premise of the claim is stale for this project's actual runtime, and the claim's blanket framing is now wrong.  WHAT CHECKS OUT - The cited page (https://developer.chrome.com/docs/web-platform/view-transitions/same-document, "Handle changes in aspect ratio") does contain, verbatim: animating those properties "is generally frowned upon in web performance circles as it runs layout per frame"; "the Chrome team plans to optimize it so it can run off the main thread in most cases"; "This optimization hasn't been implemented yet." - The page footer reads "Last updated 2024-09-25 UTC" (published 2021-08-17). So the claim's date attribu
### During animation the per-frame budget is roughly 10ms, which is usually too short to do any paint work — so a permanently-running animation that repaints (shimmer keyframes, pulse-ring) cannot fit the frame budget and must be replaced with compositor-only or event-driven animation.
**Голоса:** 0-3 · **Источник:** https://web.dev/articles/simplify-paint-complexity-and-reduce-paint-areas (primary)
> During animations, avoid paint altogether — the article gives a budget of "10ms" per frame, which it says is usually too short for paint work, especially on mobile.
**Разбор верификатора:** The 10ms number checks out; the inference built on it does not. Three independent problems.  1) OVERREACH VS. THE QUOTE. The primary source (https://web.dev/articles/simplify-paint-complexity-and-reduce-paint-areas) says: "the 10ms you have per frame is normally not long enough to get paint work done, especially on mobile devices" and "you always want to avoid paint during animations in particular." Note "normally", "especially on mobile", and — critically — the article frames this as guidance to "reduce where you can", not an impossibility proof. The article's own title and its prescribed remedies are the opposite of the claim's "must be replaced": it endorses paint-during-animation mitigat
### Native Windows acrylic via this addon is incompatible with a frameless window: it requires `frame: true` and `titleBarStyle: 'default'`, and setting `transparent: true` breaks the effect entirely. This directly conflicts with the VPN Tunnel Enforcer's frameless (-webkit-app-region: drag) window, so a vibe-style native acrylic route would require abandoning the custom frameless chrome or accepting no native backdrop.
**Голоса:** 0-3 · **Источник:** https://github.com/SoFriendly/vibe (primary)
> `frame` | `true` | must stay framed; `titleBarStyle` | `'default'` | "Windows gets fussy about frames" otherwise; `transparent` | must **not** be `true` | doing so "will disable the frame and effects will break"
**Разбор верификатора:** The quote is accurately transcribed from the vibe README, but the claim overreaches by generalizing a stale third-party addon's constraints into a project-level dichotomy ("abandon frameless chrome or accept no native backdrop"). That dichotomy is false for the actual stack (Electron 42).  1) Electron's BUILT-IN native backdrop does work on frameless windows in Electron 42. The canonical bug, electron/electron#38466 ("'backgroundMaterial' is not work in frameless window", filed May 26 2023 against Electron 24.4.0, reporting failure with `frame: false` OR `titleBarStyle: 'hidden'`), is CLOSED, fixed by electron/electron#46657 (opened by codebytere Apr 16 2025, merged to main Apr 17 2025, comm
### This is a recurring regression class rather than a one-off: the report explicitly continues earlier issues #42393 and #41824 in which the material vanished after maximizing, and the earlier fix only covered the programmatic win.maximize()/win.restore() code path, leaving user-driven window interactions broken.
**Голоса:** 1-2 · **Источник:** https://github.com/electron/electron/issues/46753 (primary)
> The reporter notes this is a continuation of earlier reports #42393 and #41824, where the material vanished after maximizing. That earlier fix only covered the programmatic path: "the issue has been partially fixed for cases involving `win.maximize()` and `win.restore()`".
**Разбор верификатора:** VERIFIED AGAINST PRIMARY SOURCE — claim holds, with one wording caveat.  1) Quote support (checklist #1). Fetching https://github.com/electron/electron/issues/46753 ("Material (Acrylic/Mica) and Rounded Corners Inconsistencies in Frameless BrowserWindow Maximize/Restore Actions", opened by aleversn 2025-04-24, Electron 35.2.1, Win11 24H2, labels 35-x-y/36-x-y, status/confirmed + has-repro-gist, assigned codebytere) confirms all three load-bearing parts: (a) it explicitly cites #42393 and #41824 — "As mentioned in issues #42393 and #41824, previous versions of Electron experienced a bug where the backgroundMaterial effect (e.g., Acrylic/Mica) would disappear after maximizing a frameless windo
### Chromium's own issue tracker carries a bug specifically about severe performance degradation from backdrop-filter: blur() applied to elements in the top-layer (i.e. <dialog>/popover — exactly the modal/overlay case a MacModal would use), confirming this is a reproduced engine-level regression rather than a general 'blur is heavy' folk belief. NOTE: the issue page itself is behind the tracker's Google sign-in wall; this text was recovered from search-engine snippets of that page, not a direct fetch.
**Голоса:** 0-3 · **Источник:** https://issues.chromium.org/issues/473056837 (primary)
> Severe performance drop when using backdrop-filter: blur(...); in the top-layer [473056837]
**Разбор верификатора:** The issue title exists, but the claim's three load-bearing assertions are each contradicted — by snippets from the same tracker page the claim cites.  1) NOT top-layer-specific, and NOT an engine "regression". Snippets from 473056837 read: "This particular issue is a known, longstanding issue in chromium's software compositor - backdrop filter as implemented currently makes two copies of the pixel buffer and applies the filter operations..." and "I'm marking this issue as a duplicate. Retrofitting software_renderer is one of my ongoing responsibilities. Please track that issue (crbug.com/40916020)". The duplicate target is titled "Rely on Skia's built-in backdrop filtering in software_render
### The Rendering panel's "Paint flashing" option is the direct instrument for detecting permanent repaints: Chromium flashes green on every repaint, so a continuously running shimmer/pulse-ring keyframe animation is visible as a persistently green region, and a whole-screen or unexpected green flash is the documented signal to investigate further. This gives a concrete, checkable way to confirm whether re-adding an animation reintroduces the repaint cost that motivated commit cfa47b5.
**Голоса:** 0-2 (воздержалось 1) · **Источник:** https://developer.chrome.com/docs/devtools/rendering/performance (primary)
> Chrome flashes the screen green whenever repainting happens. ... If, on another page, you see the whole screen flash green or areas of the screen that you didn't think should be painted, consider investigating further.
**Разбор верификатора:** The two directly-quoted parts of the claim are accurate and primary-sourced ("Chrome flashes the screen green whenever repainting happens" and "If, on another page, you see the whole screen flash green ... consider investigating further" — https://developer.chrome.com/docs/devtools/rendering/performance, mirrored in the doc's source at https://github.com/GoogleChrome/developer.chrome.com/blob/main/site/en/docs/devtools/rendering/performance/index.md). The refutation is in the inference the claim bolts onto them, which is contradicted by the actual code cfa47b5 removed.  1) The claim asserts "a continuously running shimmer/pulse-ring keyframe animation is visible as a persistently green regio
### Electron 42 bundles Chromium 148.0.7778.96, which sets the exact web-platform baseline for the project: any CSS/JS feature shipped in Chromium <= 148 (View Transitions, @starting-style, transition-behavior: allow-discrete, anchor positioning, popover, scroll-driven animations, individual transform properties, oklch/color-mix, container queries, @property) is available without a polyfill, and anything landing after 148 is not.
**Голоса:** 1-2 · **Источник:** https://www.electronjs.org/blog/electron-42-0 (primary)
> Electron 42 ships Chromium `148.0.7778.96`, V8 `14.8`, and Node `v24.15.0`
**Разбор верификатора:** CORE VERIFIED against the primary source and independently against the machine.  1) Version claim is exactly right for Electron 42.0.0. https://www.electronjs.org/blog/electron-42-0 states verbatim: "Electron 42 has been released! It includes upgrades to Chromium 148.0.7778.96, V8 14.8, and Node v24.15.0." Corroborated by the release DB (https://releases.electronjs.org/release/v42.0.0), the release schedule row "42.0.0 | Mar 12, 2026 | Apr 7, 2026 | May 5, 2026 | Oct 20, 2026 | M148 | v24.15" (https://releases.electronjs.org/schedule), and four localized mirrors (pt/zh/ja/fr). Primary source, four months old, matches a version-pinned dependency — not outdated, not marketing.  2) The enumerat
### Every backdrop-filtered element forces a readback of the destination graphics texture on each affected render pass, then filtering, clipping, and drawing that filtered image into the element's own texture before any of the element's own content paints — i.e. blur cost is per-frame GPU work in the compositor, not a one-time style computation. This is the mechanical reason a permanently visible .glass panel imposes continuous cost.
**Голоса:** 0-3 · **Источник:** https://github.com/mfreed7/backdrop-filter-feature/blob/master/README.md (primary)
> A readback of the destination graphics texture is performed. ... The filters are applied to this readback. ... The filtered image is clipped to the rounded-corner rect of the element's border. ... This image is drawn into the texture for the backdrop-filtered element, before any other contents. ... The rest of the backdrop-filtered element and children are rendered.
**Разбор верификатора:** The claim splits into two parts. Part A (the five-step mechanics: readback of destination texture -> filter -> clip to rounded-corner rect -> draw into the element's texture before its own contents -> render element and children) IS verbatim supported by the cited README. Part B — the load-bearing inference that "blur cost is per-frame GPU work in the compositor" and that this is "the mechanical reason a permanently visible .glass panel imposes continuous cost" — is an overreach and is contradicted.  1) Overreach vs. the quote. The README describes the sequence executed "when the renderer hits that render pass" — i.e. per render-pass draw, not per frame. It never says the readback happens ev
### Mica's low cost comes from a specific implementation property: it samples the desktop wallpaper only once to build its visualization rather than continuously re-blurring live content behind the window. This is Microsoft's own performance rationale for recommending Mica as the always-on foundation layer — the exact opposite of CSS backdrop-filter, which re-composites every frame. For the VPN Tunnel Enforcer this is the direct argument for Electron's backgroundMaterial: 'mica' over a permanent CSS .glass blur.
**Голоса:** 0-3 · **Источник:** https://learn.microsoft.com/en-us/windows/apps/develop/ui/system-backdrops (primary)
> Mica is designed for performance as it only captures the background wallpaper once to create its visualization, so we recommend it for the foundation layer of your app, especially in the title bar area.
**Разбор верификатора:** VERDICT: the quote is real and current, but the claim overreaches in two load-bearing ways, and its operative conclusion is wrong for this project.  WHAT SURVIVES. The supporting quote is verbatim on the primary source (learn.microsoft.com/en-us/windows/apps/develop/ui/system-backdrops, ms.date 2026-08-19, updated_at 2026-08-19 — not outdated): "Mica is designed for performance as it only captures the background wallpaper once to create its visualization, so we recommend it for the foundation layer of your app, especially in the title bar area." So "Microsoft's own performance rationale for recommending Mica as the foundation layer" is accurate.  REFUTATION 1 — "samples once" is qualified by
### box-shadow and border-radius avoid layout but still force a paint, and cheaper compositor-friendly substitutes exist: filter: drop-shadow(...) instead of boxShadow, and clipPath: inset(0 round 50px) instead of borderRadius. This gives a concrete low-cost route to depth/glass-like effects without backdrop-filter.
**Голоса:** 0-3 · **Источник:** https://motion.dev/docs/performance (primary)
> `box-shadow` and `border-radius` skip layout but "do require a potentially expensive paint (step 2)." ... prefer `filter: drop-shadow(10px 10px black)` over `boxShadow`, and `clipPath: inset(0 round 50px)` over `borderRadius`.
**Разбор верификатора:** The quote is real but the claim overreaches on three counts, and the third is the one that matters for this project.  1) SCOPE MISREAD — animation cost sold as static-rendering cost. Motion's guidance (https://motion.dev/docs/performance) is entirely about *animating* these properties: the code samples are `animate(element, { boxShadow: ... })` vs `animate(element, { filter: "drop-shadow(...)" })`. It says nothing about static depth. The claim generalizes it into "a concrete low-cost route to depth", i.e. static styling. That inversion is wrong: a static `box-shadow`/`border-radius` is painted once into the layer and cached — zero per-frame cost — whereas `filter` unconditionally promotes th
### No CSS/JS polyfill can reproduce frosted glass at production speed — existing attempts are limited in what they can filter and too slow. Any 'cheap alternative' therefore must be an approximation (layered gradients, noise, static pre-blurred snapshot, native OS material), not an equivalent live blur.
**Голоса:** 0-3 · **Источник:** https://github.com/mfreed7/backdrop-filter-feature/blob/master/README.md (primary)
> there are no effective polyfills - none can be built that are fast enough ... extremely limited in terms of what they can filter, and all are too slow for production use
**Разбор верификатора:** REFUTED on four independent grounds.  1) SOURCE IS ADVOCACY, NOT MEASUREMENT — AND MISDATED. The cited README is the "Backdrop-filter Explainer" written by mfreed7 to justify shipping the feature through the standardization/launch process. Fetched content confirms: spec "approved by the CSSWG" 25 Feb 2019, Chromium star counts "as of May 20, 2019", closing line says the feature "is implemented in Chromium as of M76" (mid-2019). The quoted sentence sits inside the motivation section ("Why is this needed? Who wants it?") and its only cited support is Reddit and StackOverflow threads — no benchmark, no frame timings, no test methodology. This is precisely the checklist's "marketing/advocacy cla
### A Chromium engineer attributes the slowdown to the software compositor path: backdrop-filter as currently implemented copies the pixel buffer twice and then applies the filter operations. This means the cost is a per-frame CPU readback/copy penalty that is incurred specifically when GPU compositing is unavailable — directly relevant to Electron on Windows machines running with GPU acceleration disabled, blocklisted drivers, or in RDP/VM sessions, where a glass panel that is cheap on a GPU-accelerated dev box becomes pathological.
**Голоса:** 0-2 (воздержалось 1) · **Источник:** https://issues.chromium.org/issues/473056837 (primary)
> This particular issue is a known, longstanding issue in chromium's software compositor - backdrop filter as implemented currently makes two copies of the pixel buffer and applies the filter operations...
**Разбор верификатора:** The quote itself is genuine and primary. Search snippets from https://issues.chromium.org/issues/473056837 ("Severe performance drop when using backdrop-filter: blur(...); in the top-layer", component Blink>Compositing>Filters) consistently reproduce it verbatim, and the same thread shows the responder saying "I'm marking this issue as a duplicate. Retrofitting software_renderer is one of my ongoing responsibilities. Please track that issue (crbug.com/40916020)" — that dupe target is https://issues.chromium.org/issues/40916020 "Rely on Skia's built-in backdrop filtering in software_renderer.cc" (a Google-domain commenter). So a Chromium engineer did say backdrop-filter in the *software* comp
### The cost is bounded by the Backdrop Root / render-surface isolation: readback only needs the nearest ancestor render surface, and no extra rasterization is required. This means constraining the backdrop-filtered element inside an isolating ancestor (a small render surface) limits how much of the page must be read back — the actionable lever for making frosted glass affordable is shrinking the isolation scope, not adding will-change.
**Голоса:** 0-3 · **Источник:** https://github.com/mfreed7/backdrop-filter-feature/blob/master/README.md (primary)
> only the nearest ancestor render surface is needed. No additional rasterization needs to take place, and this leads to a performant implementation.
**Разбор верификатора:** REFUTED on three independent grounds: the quote does not support the inference, the prescribed lever is self-defeating, and the primary source contradicts "cost is bounded."  1) OVERREACH / MISREAD OF THE QUOTE. In the explainer (https://github.com/mfreed7/backdrop-filter-feature/blob/master/README.md) "only the nearest ancestor render surface is needed. No additional rasterization needs to take place" is an argument about the *spec's* Backdrop Root definition versus the rejected alternative of reading back to the page root. It is a statement about what the UA must do given the spec, not an author-facing tuning knob. Verified by fetching the document: it never mentions isolation, will-change
### Chromium's rendering pipeline is sequential (JavaScript → Style → Layout → Paint → Composite), so an animation that touches Layout forces Paint and Composite to re-run every frame, whereas a compositor-only animation is handled entirely in the final stage — this is the mechanism behind the project's decision to kill infinite shimmer/pulse keyframes that repaint permanently.
**Голоса:** 0-3 · **Источник:** https://developer.chrome.com/docs/lighthouse/performance/non-composited-animations (primary)
> if code triggers Layout, then "the Paint and Composite steps need to run again"
**Разбор верификатора:** The claim is a compound: (A) a generic Chromium pipeline mechanism, and (B) a causal attribution ("this is the mechanism behind the project's decision to kill infinite shimmer/pulse keyframes"). (A) checks out verbatim; (B) is refuted by the project's own code.  (A) SUPPORTED. developer.chrome.com/docs/lighthouse/performance/non-composited-animations does say "The rendering pipeline consists of the following sequential steps: JavaScript, Style, Layout, Paint, Composite" and "if your code does something that triggers Layout, the Paint and Composite steps need to run again." But note the same doc defines a non-composited animation as one triggering Style, Layout, OR Paint — Layout is only one 
### transition-behavior: allow-discrete makes non-interpolatable properties (display, visibility, mix-blend-mode) flip at the 50% mark of the transition instead of at 0%, which is what allows a pure-CSS exit animation to play before the element stops rendering.
**Голоса:** 0-3 · **Источник:** https://web.dev/blog/baseline-entry-animations (primary)
> the browser now supports swapping the values at the 50% point instead of at the 0% point of a transition.
**Разбор верификатора:** The claim gets the general rule right but inverts the causal mechanism, and the cited source explicitly does not cover the case the claim is about.  1) FACTUAL ERROR — display does NOT flip at 50%; that is the named exception. MDN `transition-behavior`: "Discrete-animated properties generally flip between two values 50% through animating between the two. There is an exception, however, which is when animating to or from `display: none` or `content-visibility: hidden`. In this case, the browser will flip between the two values so that the transitioned content is shown for the entire animation duration." Spelled out: "When animating `display` from `block` ... to `none`, the value will flip to 
### Windows disables acrylic system backdrops when Battery Saver is active, while Mica keeps rendering — so a Mica-based window surface is the more resilient choice for an always-open desktop app on laptops, and any acrylic-based frosted look must be designed to degrade to a solid color at arbitrary times.
**Голоса:** 0-3 · **Источник:** https://learn.microsoft.com/en-us/windows/apps/develop/ui/materials (primary)
> **Battery Saver active** — Windows disables acrylic (`DesktopAcrylicBackdrop` and `AcrylicBrush`) when Battery Saver is on. Mica is not affected.
**Разбор верификатора:** The quote is verbatim and the source is primary and current (learn.microsoft.com/windows/apps/develop/ui/materials, ms.date 2026-08-19: "**Battery Saver active** — Windows disables acrylic (`DesktopAcrylicBackdrop` and `AcrylicBrush`) when Battery Saver is on. Mica is not affected."). The refutation is not that the quote is misread — it is that the quote is stale relative to Microsoft's own newer power documentation, and that the claim's operative conclusion ("Mica-based window surface is the more resilient choice for an always-open desktop app on laptops") does not survive.  1) CONTRADICTED BY MICROSOFT'S OWN 24H2 DOCS. https://learn.microsoft.com/en-us/windows-hardware/design/component-gui
### Blur-based paint effects (e.g. box-shadow) are measurably slower to paint than flat fills, even though CSS gives no syntactic hint of the cost difference — so a box-shadow-based 'frosted glass' imitation is not automatically a cheap substitute for backdrop-filter and must be profiled, not assumed.
**Голоса:** 0-2 (воздержалось 1) · **Источник:** https://web.dev/articles/simplify-paint-complexity-and-reduce-paint-areas (primary)
> background: red; and box-shadow: 0, 4px, 4px, rgba(0,0,0,0.5); don't necessarily look like they have vastly different performance characteristics, but they do
**Разбор верификатора:** REFUTED as an overreach on a 2015 source; only the first half survives.  1) What the source actually supports. https://web.dev/articles/simplify-paint-complexity-and-reduce-paint-areas (Paul Lewis) does say verbatim: "anything that involves a blur (like a shadow, for example) is going to take longer to paint than, say, drawing a red box", and the cited quote about `background: red` vs `box-shadow` not looking like they differ in cost. So "blur paints slower than a flat fill, and CSS gives no syntactic hint" is supported. Nothing more.  2) The load-bearing half is NOT in the source. The article never mentions `backdrop-filter` — it cannot support any comparative statement of the form "box-sha
### Both Mica and Desktop Acrylic are OS-compositor effects gated on hardware capability: they require DirectX 11 and adequate GPU memory, and devices below that threshold get a solid theme color instead. This is the platform analogue of the GPU-disabled / software-rendering case for CSS backdrop-filter.
**Голоса:** 0-3 · **Источник:** https://learn.microsoft.com/en-us/windows/apps/develop/ui/materials (primary)
> **Insufficient graphics hardware** — Mica and Desktop Acrylic require DirectX 11 and adequate GPU memory. Devices that don't meet the threshold fall back to a solid theme color.
**Разбор верификатора:** Two propositions here; the first survives, the second does not.  VERIFIED (proposition 1). I fetched the cited primary source directly (https://learn.microsoft.com/en-us/windows/apps/develop/ui/materials, ms.date/updated_at 2026-08-19, i.e. 10 days old — not stale). The quote is verbatim and in context: under "No material (default)" the doc lists fallback conditions, including "Insufficient graphics hardware — Mica and Desktop Acrylic require DirectX 11 and adequate GPU memory. Devices that don't meet the threshold fall back to a solid theme color." No credible source disputes this; the same text appears on Microsoft's localized mirrors. Primary-source quality matches the strength of a hardw
### Earlier Electron fixes for backgroundMaterial loss after maximize (issues #42393, #41824) only covered the programmatic API path, not user-driven maximize/restore, so the defect persisted across releases.
**Голоса:** 0-3 · **Источник:** https://github.com/electron/electron/issues/46753 (primary)
> the issue has been partially fixed for cases involving `win.maximize()` and `win.restore()`
**Разбор верификатора:** Two specific problems: (a) the causal framing overreaches the source and is contradicted by the source's own repro matrix; (b) the claim is stale for the stack under study (Electron 42).  1) Quote support is partial, mechanism is invented. The supporting quote from https://github.com/electron/electron/issues/46753 says only that the bug "has been partially fixed for cases involving `win.maximize()` and `win.restore()`" and that "the material effect still fails under other conditions." It does not say the earlier fix "only covered the programmatic API path." That is the voter-side inference, not the reporter's statement.  2) The issue's own gesture matrix contradicts a clean programmatic-vs-u
### Electron's Windows 11 backgroundMaterial ('acrylic') effect is lost on a frameless BrowserWindow when the window is maximized or restored by drag-to-maximize or title-bar double-click, while the same actions via the window buttons preserve it — reported on Electron 35.2.1 / Windows 11 24H2 with frame: false and backgroundMaterial: 'acrylic'.
**Голоса:** 0-3 · **Источник:** https://github.com/electron/electron/issues/46753 (primary)
> Drag to maximize → drag to restore | lost ("incorrect background color") | preserved
**Разбор верификатора:** REFUTED ON CURRENCY/APPLICABILITY, not on historical accuracy.  1) The quote does support the historical report. electron/electron#46753 ("Windows 11: Material (Acrylic/Mica) and Rounded Corners Inconsistencies in Frameless BrowserWindow Maximize/Restore Actions", opened by aleversn 2025-04-24, Electron 35.2.1, Windows 11 24H2, `frame: false` + `backgroundMaterial: "acrylic"`, label `status/confirmed`, i.e. maintainer-reproduced) contains exactly the table row cited: drag maximize -> drag restore = material lost, and double-click maximize -> double-click restore = material lost, vs button maximize/restore = preserved. So source quality is fine (primary, confirmed) for what actually happened 
### Only transform and opacity avoid triggering paint; animating any other CSS property always causes a repaint. This directly justifies restricting the redesign's animations to transform/opacity and treating background/color/shadow animation as a paint cost.
**Голоса:** 0-3 · **Источник:** https://web.dev/articles/simplify-paint-complexity-and-reduce-paint-areas (primary)
> Changing any property apart from transforms or opacity always triggers paint.
**Разбор верификатора:** REFUTED on three independent grounds: the source is 11 years stale, its absolute wording is contradicted by Chromium's own documentation, and the claim silently drops the load-bearing qualifier from that same web.dev series.  1) Source age. https://web.dev/articles/simplify-paint-complexity-and-reduce-paint-areas (Paul Lewis) shows "Last updated: 2015-03-20 UTC". Its sibling, https://web.dev/stick-to-compositor-only-properties-and-manage-layer-count/ (same 2015-03-20 date), says "Today there are only two properties for which that is true - transforms and opacity" — note "Today", i.e. an explicitly time-scoped statement about 2015 Chromium, now cited as a timeless rule about Electron 42 (Chro
### Every render pass containing a backdrop-filter element forces a readback of the destination GPU texture before the element paints — the cost is per-frame readback + filter + rounded-rect clip, not one-time. This is the structural reason a permanently visible .glass panel is expensive in a constantly-open Electron window.
**Голоса:** 0-3 · **Источник:** https://github.com/mfreed7/backdrop-filter-feature/blob/master/README.md (primary)
> A readback of the destination graphics texture is performed.
**Разбор верификатора:** The quote does not support the claim, and the load-bearing part of the claim ("per-frame, not one-time") is contradicted by Chromium's own compositor code.  1) OVERREACH ON THE QUOTE. mfreed7's explainer lists five steps that occur "when that render pass is hit in the renderer" — readback, filter, rounded-rect clip, draw, then children. It never says per-frame. The same document argues the opposite of the claim's conclusion: because the Backdrop Root scopes the readback, "only the nearest ancestor render surface is needed", "No additional rasterization needs to take place, and this leads to a performant implementation", and authors get the effect "without incurring a significant performance 
### Chromium's current backdrop-filter implementation makes two full copies of the pixel buffer before applying the filter, so the cost scales with the blurred backdrop AREA rather than with the complexity of the glass element's own content — making large full-panel .glass surfaces the worst case.
**Голоса:** 0-3 · **Источник:** https://issues.chromium.org/issues/473056837 (primary)
> backdrop filter as implemented currently makes two copies of the pixel buffer and applies the filter operations
**Разбор верификатора:** The claim materially overreaches its own source on two counts, and the source is the wrong kind of document for the strength of the claim.  1) SCOPE STRIPPED — the quote is about the SOFTWARE compositor, not "Chromium's current backdrop-filter implementation" generally. The full sentence in issue 473056837, as surfaced verbatim in search indexing of the tracker, reads: "This particular issue is a known, longstanding issue in chromium's software compositor - backdrop filter as implemented currently makes two copies of the pixel buffer and applies the filter operations..." The claim quotes only the second clause and drops "in chromium's software compositor," converting a statement about the fa
### Whether a given animation is compositor-driven is directly observable, not a matter of inference: Chrome emits per-animation compositing failure reasons into the DevTools trace, and Lighthouse reads that trace to list the offending DOM nodes together with the reason each failed to composite.
**Голоса:** 1-2 · **Источник:** https://developer.chrome.com/docs/lighthouse/performance/non-composited-animations (primary)
> Chrome reports the failure reasons to the DevTools trace, which is what Lighthouse reads.
**Разбор верификатора:** Attempted refutation on five fronts; the core mechanism survived all of them.  1) Quote support — exact, not overreach. The primary page (https://developer.chrome.com/docs/lighthouse/performance/non-composited-animations) states both halves independently: "When an animation can't be composited, Chrome reports the failure reasons to the DevTools trace, which is what Lighthouse reads" AND that the audit "lists DOM nodes which have animations that were not composited along with the failure reason(s)". The claim's two assertions map 1:1.  2) Independent corroboration inside DevTools itself (so "directly observable" is not Lighthouse-only). The DevTools Performance panel reference (https://develo
### Native acrylic requires the renderer background to be transparent on BOTH html and body, plus `backgroundColor: '#00000000'` on the BrowserWindow — meaning the app cannot keep an opaque CSS panel/gradient background (the exact thing commit cfa47b5 moved toward with `rgb(card / 0.96)`) while also using a native backdrop.
**Голоса:** 0-3 · **Источник:** https://github.com/SoFriendly/vibe (primary)
> CSS side: both `html` and `body` need `background: transparent`. Setting only one is called out as "a common mistake." ... `backgroundColor` | `'#00000000'` | tricks Electron into a framed transparent window
**Разбор верификатора:** The claim fails on three independent grounds: wrong mechanism, wrong requirements for the mechanism the project would actually use, and a non-sequitur conclusion.  1) SOURCE MISMATCH — vibe is not "native acrylic" in Electron 42. SoFriendly/vibe (a 0-star fork of pykeio/vibe, Rust/N-API native addon, no mention of Electron's `backgroundMaterial` anywhere in its README) implements acrylic via a stack of hacks it credits itself: `DwmExtendFrameIntoClientArea`, the `enable-transparent-visuals` switch, a "workaround to avoid `transparent: true`", borrowed Tauri `window-vibrancy` code, and for Mica on pre-22523 builds an explicitly "undocumented API" that is "not heavily tested and may not work a
### The severe backdrop-filter: blur() performance drop reported in Chromium issue 473056837 is scoped to Chromium's SOFTWARE compositor, not the GPU-accelerated path — meaning glass effects can be acceptable with hardware acceleration but collapse when Electron falls back to software rendering (disabled/blocklisted GPU, RDP, --disable-gpu).
**Голоса:** 0-3 · **Источник:** https://issues.chromium.org/issues/473056837 (primary)
> This particular issue is a known, longstanding issue in chromium's software compositor - backdrop filter as implemented currently makes two copies of the pixel buffer and applies the filter operations...
**Разбор верификатора:** The claim is two statements welded together; the first is supported, the second is an overreach that other Chromium bugs contradict.  WHAT HOLDS UP. The quote is real and the scoping is corroborated. Search-index snippets of https://issues.chromium.org/issues/473056837 ("Severe performance drop when using backdrop-filter: blur(...); in the top-layer") reproduce the cited comment verbatim, and a second snippet from the same issue's resources page shows it was closed as a duplicate: "I'm marking this issue as a duplicate. Retrofitting software_renderer is one of my ongoing responsibilities. Please track that issue (crbug.com/40916020)". Issue 40916020 is titled "Rely on Skia's built-in backdro
### Setting backgroundMaterial/vibrancy forces the window's background color to transparent and strips caption styling, so the app's own CSS/window background must be transparent for the native effect to show — a direct conflict with an opaque panel surface like the project's rgb(card / 0.96) background.
**Голоса:** 0-3 · **Источник:** https://github.com/electron/electron/pull/39708 (primary)
> Windows created with vibrancy/translucency automatically get "transparent background colors" ... "Removes some caption styling on translucent window effects on Windows when setting background material"
**Разбор верификатора:** The claim has two verbatim-supported halves and one load-bearing conclusion that is an overreach and is contradicted by Microsoft's own material guidance.  WHAT IS SUPPORTED. Electron PR #39708 (merged 2023-09-11, backported to 27-x-y as #39802, release note "Fixed Windows Mica / Acrylic background material effects on frameless windows") does state that (a) "windows created with vibrancy or translucency effects automatically get transparent background colors" rather than requiring the developer to set them, and (b) it "Removes some caption styling on translucent window effects on Windows when setting background material". Note the framing: (a) is a convenience — Electron sets the *native win
### The "Frame rendering stats" checkbox pins an overlay in the viewport's top-right corner that reports four measurable quantities at once: a live FPS estimate, a frame timeline classifying frames as successfully rendered (blue), partially presented (yellow), or dropped (red), whether GPU rasterization is on or off, and GPU memory used versus maximum in MB. This is the built-in way to verify GPU-accelerated compositing state and dropped-frame counts on Windows, including with GPU acceleration disabled.
**Голоса:** 0-3 · **Источник:** https://developer.chrome.com/docs/devtools/rendering/performance (primary)
> in the top-right corner of your viewport
**Разбор верификатора:** The descriptive half checks out; the load-bearing half is added by the claim author and is not in the source — and is partly wrong on Chromium terminology.  SUPPORTED by the primary source (https://developer.chrome.com/docs/devtools/rendering/performance): "The Frame rendering stats is an overlay that appears in the top-right corner of your viewport"; it lists exactly four items — FPS ("Real time estimate of frames per second as the page runs"), Frame timeline ("Successfully rendered frames (blue lines)", "Partially presented frames (yellow lines)", "Dropped frames (red lines)"), "GPU rasterization: The state of the GPU raster: on or off", and "GPU memory: GPU memory usage: the number of use
### Chromium's rendering pipeline has five sequential stages (JavaScript, Style, Layout, Paint, Composite), and a non-composited animation is defined as any animation that triggers one of the stages earlier than Composite — i.e. Style, Layout, or Paint — forcing the browser to do strictly more per-frame work.
**Голоса:** 0-3 · **Источник:** https://developer.chrome.com/docs/lighthouse/performance/non-composited-animations (primary)
> A non-composited animation is any animation that triggers one of the earlier steps
**Разбор верификатора:** The claim fuses a correct paraphrase with two overreaches, and both are contradicted by primary Chromium sources.  1) "Chromium's rendering pipeline has five sequential stages" is false as a statement about Chromium. The five-box model is web.dev's pedagogical "pixel pipeline" ("There are five major areas that you need to know about... as a web developer", https://web.dev/articles/rendering-performance, last updated 2023-12-13). Chromium's own architecture documentation enumerates **12** pipeline stages: Animate, Style, Layout, Pre-paint, Scroll, Paint, Commit, Layerize, "Raster, decode and paint worklets", Activate, Aggregate, Draw (https://developer.chrome.com/docs/chromium/renderingng-arc
### The cost bound comes from the Backdrop Root / nearest-ancestor-render-surface rule, not from author hints: only the nearest ancestor render surface is read back, and no extra rasterization is needed. This implies isolating the blurred element inside a small render surface shrinks the readback region, whereas will-change alone is not described as reducing readback cost.
**Голоса:** 0-3 · **Источник:** https://github.com/mfreed7/backdrop-filter-feature/blob/master/README.md (primary)
> only the nearest ancestor render surface is needed
**Разбор верификатора:** The two verbatim fragments check out, but the load-bearing inference does not, and the source cannot carry a cost-bound claim in 2026.  1) WHAT THE SOURCE ACTUALLY SAYS (verified by fetch of https://github.com/mfreed7/backdrop-filter-feature/blob/master/README.md): at readback step 1, "only the nearest ancestor render surface is needed", and "No additional rasterization needs to take place, and this leads to a performant implementation." Confirmed present.  2) OVERREACH — the actionable half is absent from the source. The explainer never says readback cost scales with render-surface size, never says isolating the element in a smaller surface shrinks readback, and the words "will-change", "co
### Microsoft explicitly restricts Acrylic — the true frosted-glass, semi-transparent, live-content-blurring material — to transient, light-dismiss surfaces (flyouts, context menus), not to persistent app windows or panels. This is a vendor-level design constraint matching the project's own cfa47b5 rollback reasoning: frosted glass is acceptable on short-lived surfaces (modals, popovers, toasts) but not as a permanent panel surface.
**Голоса:** 0-3 · **Источник:** https://learn.microsoft.com/en-us/windows/apps/develop/ui/system-backdrops (primary)
> Acrylic is a semi-transparent material that replicates the effect of frosted glass. It's used only for transient, light-dismiss surfaces such as flyouts and context menus.
**Разбор верификатора:** The supporting quote is real and current (page ms.date 2026-08-19), but the claim built on it is an overreach that the SAME primary page and its own linked design page directly contradict.  1) Contradicted on the identical page. https://learn.microsoft.com/en-us/windows/apps/develop/ui/system-backdrops contains a section titled "Acrylic" whose body reads verbatim: "Desktop Acrylic can be used as the backdrop for a Window." with a code sample `<Window.SystemBackdrop><DesktopAcrylicBackdrop/></Window.SystemBackdrop>`. The next section is separately titled "Acrylic on transient UI" ("Acrylic is also commonly used as the backdrop for transient UI, like a flyout") — the word "also" establishes tr
### The defect is not limited to the backdrop: in the worst reproduction sequence (button maximize followed by drag-to-restore) the frameless window loses BOTH the acrylic material and the Windows 11 rounded corners, so adopting backgroundMaterial in a frameless app risks visible window-shape regressions, not just a flat background.
**Голоса:** 0-3 · **Источник:** https://github.com/electron/electron/issues/46753 (primary)
> Expected behavior, per the report: the effect "should persist in **all** maximize/restore scenarios," corners "should remain consistent," and all materials (including mica) ought to be supported.
**Разбор верификатора:** Two independent defects in the claim: the quote does not support it, and the risk assertion is outdated for this project's Electron version.  1) Quote mismatch / overreach as sourced. The supplied supporting quote is the issue's EXPECTED-behavior paragraph ("should persist in all maximize/restore scenarios", corners "should remain consistent", mica "ought to be supported"). That text states what the reporter wanted, not what was observed, so it cannot establish "loses BOTH the acrylic material and the rounded corners". The factual support exists elsewhere in the issue — I fetched https://github.com/electron/electron/issues/46753 and it does contain a per-sequence repro table: title-bar butto
### content-visibility: auto skips layout AND paint work for offscreen elements entirely, rendering them just-in-time as they approach the viewport — directly applicable to the long server list in Servers.tsx without adding a virtualization library.
**Голоса:** 0-3 · **Источник:** https://web.dev/articles/content-visibility (primary)
> enables the user agent to skip an element's rendering work, including layout and painting, until it is needed
**Разбор верификатора:** The first half is accurate; the operative second half ("directly applicable to the long server list in Servers.tsx without adding a virtualization library") is an overreach unsupported by the cited quote and contradicted by other evidence.  1) Quote vs claim. The web.dev quote covers only the mechanism (skip rendering work incl. layout+paint until needed). It says nothing about lists, React, or virtualization. web.dev's own virtualization section positions `content-visibility: hidden` as a primitive FOR building "advanced virtual scrollers" and for keeping inactive SPA views in the DOM — never as a substitute for one. The word "entirely" is also imprecise: per MDN, `auto` skips descendant la
## Не рассмотрено (3)

### Windows disables Acrylic entirely when Battery Saver is active, while Mica is unaffected — so an Electron app relying on backgroundMaterial:'acrylic' will silently lose its frosted-glass look on battery-saving laptops, whereas 'mica' survives.
**Голоса:** 0-1 (воздержалось 2) · **Источник:** https://learn.microsoft.com/en-us/windows/apps/develop/ui/materials (primary)
> **Battery Saver active** — Windows disables acrylic (`DesktopAcrylicBackdrop` and `AcrylicBrush`) when Battery Saver is on. Mica is not affected.
### Windows system materials (Mica / Desktop Acrylic) are drawn by the OS compositor, not by the app's own renderer — which is exactly what Electron's BrowserWindow backgroundMaterial ('mica'/'acrylic'/'tabbed') maps onto, meaning the blur cost is borne by DWM rather than by Chromium's paint/composite pipeline.
**Голоса:** 0-0 (воздержалось 3) · **Источник:** https://learn.microsoft.com/en-us/windows/apps/develop/ui/materials (primary)
> Materials rely on compositor effects that require hardware support and user preferences.
### If the user turns off Transparency effects in Settings > Personalization > Colors, every system backdrop material falls back to a solid color — therefore any design built on native Mica/Acrylic must still read correctly as a fully opaque surface, and the fallback color is the design that must be gotten right.
**Голоса:** 0-0 (воздержалось 3) · **Источник:** https://learn.microsoft.com/en-us/windows/apps/develop/ui/materials (primary)
> **Transparency effects disabled** — if the user turns off *Transparency effects* in **Settings &gt; Personalization &gt; Colors**, all `SystemBackdrop` materials and `AcrylicBrush` fall back to their solid-color alternatives.